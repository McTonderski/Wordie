import 'package:flutter/material.dart';

class NavBarWordie extends StatelessWidget {
  const NavBarWordie({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AppBar(
      
    )
  }
}